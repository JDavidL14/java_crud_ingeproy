create table alumno(
	n int identity,
	carrera varchar(5),
	anio varchar(2),
	correlativo varchar(6) primary key,
	pnombre varchar(30),
	snombre varchar(30),
	papellido varchar(30),
	sapellido varchar(30),
	telefono varchar(8),
	email varchar(30),
	fenacimiento date,
	status int
)